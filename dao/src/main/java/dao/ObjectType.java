package dao;

/**
 * Enum which define all Elements which compose the map
 * @version 1.0
 */
public enum ObjectType {
    DIRT,
    ROCK,
    DIAMOND,
    MONSTER,
    WALL,
    CHARACTER,
    EXIT,
    TEST
}
