package world.behavior;

public class DisplacementBehavior extends Behavior {

    protected IDisplacementBehavior elements;

    public DisplacementBehavior(IDisplacementBehavior elements) {
        super(elements);
    }
}
